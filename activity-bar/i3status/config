general {
        colors = true
        interval = 10
}

order += "volume master"
order += "battery all"
order += "memory"
order += "tztime local"

battery all {
        format = "%status %percentage - %remaining"
        integer_battery_capacity = true
        status_chr = "🔌"
        status_bat = ""
        status_full = ""
        low_threshold = 40
}

# Will get colored when the free memory is less than 3G
memory {
        format = " %used"
        threshold_degraded = "3G"
        format_degraded = " %used"
}

tztime local {
        format = "%d-%m %H:%M:%S"
}

volume master {
        format = " %volume"
        format_muted = ""
        device = "default"
        mixer = "Master"
        mixer_idx = 0
}