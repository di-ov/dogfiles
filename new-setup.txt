
Workflow ideas:
1. Have a pre configured layouts i.e. ( 2horiz 1 vert)
2. Multy monitor config
Extra
For automatic screen config - https://github.com/phillipberndt/autorandr

# All external binaries are installed in /usr/local/bin --> make an env var for that

# Screenshoter maim

1.
# Update resources list. Replace /etc/apt/sources.list
# We want to use main and contrin repos only (I think)
# For Apt pinning: 
https://wiki.debian.org/AptConfiguration?action=show&redirect=AptPreferences
https://serverfault.com/questions/240920/how-do-i-enable-non-free-packages-on-debian
https://unix.stackexchange.com/questions/284582/how-to-block-non-free-with-apt-preferences?noredirect=1&lq=1

# Install microcode. For why is it needed refer to https://wiki.debian.org/Microcode
apt-get install intel-microcode

# Install various packages
sudo apt-get install man-db
sudo apt-get install curl
sudo apt install xorg
sudo apt install xss-lock
sudo apt install xbacklight -- explain how to do the config

# SEE HOW to install themes

# How to install i3 
check the wobsite

sudo apt install lm-sensors
sudo apt install pcmanfm
sudo apt install htop

# Install micro (install )
curl https://getmic.ro | bash
sudo mv micro /usr/local/bin

# Mostly work:

For open vpn to work:
sudo apt install openvpn 
sudo apt install network-manager-openvpn-gnome

# Kubectl, Skaffold, Helm, Minikube, docker, docker-compose, node, Db client (datagrip)
Check websites

# Teams, Zoom
download deb package

